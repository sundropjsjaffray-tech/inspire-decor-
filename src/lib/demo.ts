/**
 * Demo-data markers.
 *
 * Every seeded record in `~/lib/data` is flagged `isDemo: true` and the whole
 * prototype runs against clearly-labelled demonstration data. The notice below
 * is rendered by the layouts and can be shown anywhere that needs to remind
 * the owner that the numbers are not real client records.
 */

export const DEMO_DATA_NOTICE =
  "Demonstration data — sample records for preview only, not real client information.";

export const DEMO_DATA_BADGE = "DEMO";
